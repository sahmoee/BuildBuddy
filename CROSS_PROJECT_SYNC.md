# Cross-project sync

BuildBuddy may operate on every sibling repository but is not a runtime dependency of them. Keep Git, branch, Xcode, versioning, and deployment behavior generic. If a project-specific workflow is added, document it here without embedding that project's credentials or business logic.
