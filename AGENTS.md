# AI instructions

Before editing, read `README_FIRST.md`, then `CROSS_PROJECT_SYNC.md`. Read only the relevant README/code sections afterward. Preserve secrets and unrelated user changes. Keep this local-first and dependency-light. Verify the changed launcher/build path. Update these guides whenever behavior, setup, or shared workflow changes.

Build validation for app or web UI changes must retain each product's themed-presentation and adaptive-layout requirements: complete themed surfaces, flexible window/device sizing, Dynamic Type or browser zoom support, accessible hit targets, and scrolling only where content requires it. Do not normalize intentional media geometry into layout-policy violations.
