# Read me first

BuildBuddy is a local macOS Git/Xcode utility implemented primarily in `BuildBuddy.swift` with `.command` launch/build scripts. It must work without accounts or a developer-controlled service. Do not add cloud dependencies or hidden repository mutations.

For changes, inspect the single affected workflow, preserve recovery/undo behavior, and run the relevant launcher or app build.
